<section class="cart-show">

  <header class="page-header">
    <h1>My Cart</h1>
  </header>

  <%= render 'cart_review', cart: cart unless cart.size == 0 %>
  <%= render 'empty_cart_message' if cart.size == 0 %>

</section>
